const config = {
  /* don't expose password or any sensitive info, done only for demo */
  host: "localhost",
  user: "root",
  password: null,
  database: "tododoo",
  dialect: "mysql",
};
module.exports = config;
